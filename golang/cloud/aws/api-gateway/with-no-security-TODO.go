// demonstrate to do the below:
// register an api gateway (fo example as a trigger to lambda function)
// choose the security as "Open"
// create a client in go and call the api gateway endpoint
//
// note: this is no different than calling any public webservice
