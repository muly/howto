// using github.com/google/go-licenses to extract the licenses information within the project code repo. the project code and the vendor branch

// example project: 
// simple guid number generator code
