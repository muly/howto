// given an template layout file and some html text, use standard library templates to bund these two and return a html content
// write a function to do the above.
// func htmlPageGen(layoutFile, htmlContent)htmlData{}
// choose a template layout file that has a header section, footer section, side navigation bar, and area at the centre of the page to hold the dynamic content 
//

